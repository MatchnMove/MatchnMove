{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "preDeployCommand": "npm run prisma:migrate:deploy && if [ \"$SEED_DEMO_MOVERS\" = \"true\" ]; then npm run seed:demo-movers; fi",
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
